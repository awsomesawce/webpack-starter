const fs = require('fs')
const path = require('path')
.exit
.lodash
.lodash swap
_.set
_.templateSettings
_.templateSettings.variable
_.templateSettings.imports._
const fs = require('fs')
const path = require('path')
const util = require('util')
const vm = require('vm')
require.cache
let myPaths = [
"C:\\Users\\Carl\\scoop\\buckets\\main"]
let ls = dir => {
return fs.readdirSync(dir)
}
ls(myPaths[0])
const yaml = require('js-yaml')
let myDir = ls(myPaths[0])
let myFile = path.resolve(myPaths[6])
let myFile = path.resolve(myDir[6])
myFile = path.resolve(myDir[6])
let fileOne;
fileOne = path.resolve(myDir[6])
myPaths[0]
myDir = fs.readdirSync(path.resolve(myPaths[0]))
let myArr = []
for (let i of myDir) {
myArr.push(`"${myPaths[0]}\\${i}`)
}
myArr
myFile = myArr[6]
fileOne = myArr[6]
yaml.load(fs.readFileSync(fileOne).toLocaleString)
myArr.join('hello')
myArr.clear
myArr.splice('hello')
myArr.splice('main')
myArr
for (let i of myDir) {
myArr.push(`${myPaths[0]\\${i}`)
for (let i of myDir) {
myArr.push(`${myPaths[0]}\\${i}`)}
myArr.6
myArr
myArr[6]
yaml.safeLoad.toLocaleString()
let text;
text = fs.readdirSync(myArr[6]).toLocaleString()
text = fs.readFileSync(myArr[6]).toLocaleString()
text
yaml.load(text)
let fromYamlFile;
fromYamlFile = yaml.load(text)
fromYamlFile.environment
fromYamlFile.matrix
fromYamlFile
fromYamlFile.environment
fromYamlFile.environment.matrix
fromYamlFile.environment.matrix[0]
const json5 = require('json5')
.exit
const fs = require('fs')
const path = require('path')
.exit
.lodash
.lodash swap
_.set
_.templateSettings
_.templateSettings.variable
_.templateSettings.imports._
const fs = require('fs')
const path = require('path')
const util = require('util')
const vm = require('vm')
require.cache
let myPaths = [
"C:\\Users\\Carl\\scoop\\buckets\\main"]
let ls = dir => {
return fs.readdirSync(dir)
}
ls(myPaths[0])
const yaml = require('js-yaml')
let myDir = ls(myPaths[0])
let myFile = path.resolve(myPaths[6])
let myFile = path.resolve(myDir[6])
myFile = path.resolve(myDir[6])
let fileOne;
fileOne = path.resolve(myDir[6])
myPaths[0]
myDir = fs.readdirSync(path.resolve(myPaths[0]))
let myArr = []
for (let i of myDir) {
myArr.push(`"${myPaths[0]}\\${i}`)
}
myArr
myFile = myArr[6]
fileOne = myArr[6]
yaml.load(fs.readFileSync(fileOne).toLocaleString)
myArr.join('hello')
myArr.clear
myArr.splice('hello')
myArr.splice('main')
myArr
for (let i of myDir) {
myArr.push(`${myPaths[0]\\${i}`)
for (let i of myDir) {
myArr.push(`${myPaths[0]}\\${i}`)}
myArr.6
myArr
myArr[6]
yaml.safeLoad.toLocaleString()
let text;
text = fs.readdirSync(myArr[6]).toLocaleString()
text = fs.readFileSync(myArr[6]).toLocaleString()
text
yaml.load(text)
let fromYamlFile;
fromYamlFile = yaml.load(text)
fromYamlFile.environment
fromYamlFile.matrix
fromYamlFile
fromYamlFile.environment
fromYamlFile.environment.matrix
fromYamlFile.environment.matrix[0]
const json5 = require('json5')
.exit
.load .n_myhist.js
const json5 = require('json5')
.exit
